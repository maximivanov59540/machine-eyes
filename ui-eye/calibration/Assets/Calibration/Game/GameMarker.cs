namespace Calibration.Game
{
    /// <summary>
    /// Сборка с движком — мишень калибровки ui-eye: часть «компиляция» кладёт в эту папку файл с нарочной ошибкой и убирает
    /// его после прогона (<c>calibrate.js --сборка-ошибки</c>). Тип нужен, чтобы сборка не была пустой.
    /// </summary>
    internal static class GameMarker
    {
    }
}
